{% extends 'base.html.twig' %}

{% block title %}Prendre index{% endblock %}

{% block body %}
    <h1>Prendre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for prendre in prendres %}
            <tr>
                <td>{{ prendre.id }}</td>
                <td>
                    <a href="{{ path('prendre_show', {'id': prendre.id}) }}">show</a>
                    <a href="{{ path('prendre_edit', {'id': prendre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('prendre_new') }}">Create new</a>
{% endblock %}
