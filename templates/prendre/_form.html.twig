{{ form_start(form) }}
<div class="form-group">
    {{ form_widget(form,{'class':'form-control'}) }}
</div>
<button class="btn btn-primary"><i class="fa fa-plus"></i> {{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
