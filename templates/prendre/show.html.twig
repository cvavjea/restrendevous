{% extends 'loaderTemplate.html.twig' %}

{% block title %}Rende-vous|Rendez-vous{% endblock %}

{% block main %}

<div class="col-xl-8 col-lg-7">
    <div class="card bg-success text-white shadow mb-4">
        <!-- Card Header - Dropdown -->
        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
            <h6 class="m-0 font-weight-bold text-primary">Détailes un rende-vous</h6>
            <div class="dropdown no-arrow">
                <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in" aria-labelledby="dropdownMenuLink">
                    <div class="dropdown-header">Dropdown Header:</div>
                </div>
            </div>
        </div>
        <!-- Card Body -->
        <div class="card-body">

    <table class="table table-striped table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prendre.id }}</td>
            </tr>
        </tbody>
    </table>

            <a class="btn btn-primary" href="{{ path('prendre_index') }}"><i class="fa fa-list"></i> Liste</a>

    <a class="btn btn-warning" href="{{ path('prendre_edit', {'id': prendre.id}) }}"><i class="fa fa-edit"></i> Modifier</a>

    {{ include('prendre/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}

