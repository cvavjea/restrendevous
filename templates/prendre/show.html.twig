{% extends 'base.html.twig' %}

{% block title %}Prendre{% endblock %}

{% block body %}
    <h1>Prendre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prendre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prendre_index') }}">back to list</a>

    <a href="{{ path('prendre_edit', {'id': prendre.id}) }}">edit</a>

    {{ include('prendre/_delete_form.html.twig') }}
{% endblock %}
